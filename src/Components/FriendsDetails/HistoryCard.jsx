'use client'
import { DataContext } from '@/context/DataContextProvider';
import React, { useContext } from 'react';

const HistoryCard = ({ id }) => {
  const {callDta} = useContext(DataContext)
  return (
    <div>
      
    </div>
  );
};

export default HistoryCard;
